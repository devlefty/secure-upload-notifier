// 该文件是项目的入口点，负责初始化上传通知功能，并导出主要的通知类或函数。

import { Notifier, createNotifier } from "./notifier/index";
import { NotifierConfig } from "./types/index";

export { Notifier, NotifierConfig, createNotifier };
